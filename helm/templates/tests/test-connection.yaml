apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "studentai.fullname" . }}-test-connection"
  labels:
    {{- include "studentai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "studentai.fullname" . }}-backend:{{ .Values.backend.service.port }}']
  restartPolicy: Never
